# Example .env configuration file
# COPY THIS FILE AS .env AND CHANGE ALL VALUES TO SECURE ONES!

# ============================================
# ENCRYPTION CONFIGURATION (REQUIRED!)
# ============================================
# GENERATE NEW KEYS USING:
# Linux/macOS: openssl rand -base64 48
# Windows: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})

# Pepper for password hashing (minimum 32 characters) - also used for tokens
PEPPER=CHANGE_THIS_TO_RANDOM_SECURE_KEY_MIN_32_CHARS

# Master key for encrypting TOTP secrets (minimum 32 characters)
ENCRYPTION_SECRET=CHANGE_THIS_TO_RANDOM_SECURE_KEY_MIN_32_CHARS

# JWT secret (minimum 32 characters)  
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECURE_KEY_MIN_32_CHARS

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_HOST=database
DB_PORT=5432
DB_USER=user
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
DB_NAME=communicator_db

# ============================================
# SMTP CONFIGURATION
# ============================================
# Choose the appropriate configuration for your email provider:

# --- Gmail ---
# SMTP_ADDR=smtp.gmail.com:587
# SMTP_HOST=smtp.gmail.com
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-char-app-password
# SMTP_TLS_SERVER_NAME=smtp.gmail.com
# SMTP_FROM_EMAIL=your-email@gmail.com
# SMTP_FROM_NAME=Communicator

# --- Outlook/Office365 ---
# SMTP_ADDR=smtp.office365.com:587
# SMTP_HOST=smtp.office365.com
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password
# SMTP_TLS_SERVER_NAME=smtp.office365.com
# SMTP_FROM_EMAIL=your-email@outlook.com
# SMTP_FROM_NAME=Communicator

# --- SendGrid ---
# SMTP_ADDR=smtp.sendgrid.net:587
# SMTP_HOST=smtp.sendgrid.net
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_TLS_SERVER_NAME=smtp.sendgrid.net
# SMTP_FROM_EMAIL=noreply@yourdomain.com
# SMTP_FROM_NAME=Communicator

# --- Custom SMTP server ---
SMTP_ADDR=smtp.example.com:587
SMTP_HOST=smtp.example.com
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_TLS_SERVER_NAME=smtp.example.com
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Communicator
